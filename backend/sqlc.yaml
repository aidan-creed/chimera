version: "2"
sql:
  # --- Generic Platform Querier ---
  - schema: 
      - "sql/platform/migrations/"
      - "sql/internal-sql/temp_tables.sql"
    queries: "sql/platform/queries/"
    engine: "postgresql"
    gen:
      go:
        package: "repository"
        out: "internal/repository"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true

  # --- Public Application-Specific Querier ---
  - schema:
      - "sql/platform/migrations/"
      - "sql/apps/chimera/migrations/"
    queries: "sql/apps/chimera/queries/"
    engine: "postgresql"
    gen:
      go:
        package: "demo"
        out: "internal/apps/demo"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
