-- backend/sql/dev/seed_dev_user.sql.example

-- Use this script to create a local development user.
-- Copy this file to 'seed_dev_user.sql' (which is git-ignored)
-- and replace the placeholder values with your own.

INSERT INTO "users" (auth_provider_subject, email, display_name, is_active, is_admin)
VALUES ('Identity_Provider|your-test-subject-here', 'your.email@example.com', 'Your Name', TRUE, TRUE)
ON CONFLICT (auth_provider_subject) DO NOTHING;

-- You can also assign scopes or roles here if needed
-- INSERT INTO "user_scope_access" (user_id, scope) VALUES ((SELECT id FROM "users" WHERE email = 'your.email@example.com'), 'your-dev-scope');
